# Binaries
/bin/
*.exe
*.dll
*.so
*.dylib

# Test and coverage
*.test
*.out
coverage.html

# WASM build artifacts — pre-built binaries are committed for Cloudflare Pages
# (Pages has no Go build step; rebuild locally with:
#   GOOS=js GOARCH=wasm go build -o demo/wimse-agent.wasm ./cmd/demo-wasm/
#   cp $(go env GOROOT)/lib/wasm/wasm_exec.js demo/ )
# demo/wimse-agent.wasm  ← tracked (intentional)
# demo/wasm_exec.js      ← tracked (intentional)

# Go workspace
/vendor/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
